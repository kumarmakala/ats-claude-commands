Implement tenant-specific enterprise SSO following ADR-003.

OIDC requirements:

- authorization-code flow
- PKCE where applicable
- state and nonce validation
- issuer and audience validation
- discovery metadata
- key rotation
- tenant discovery using verified domain or explicit safe entry point

SAML requirements:

- signed assertions
- audience and recipient validation
- InResponseTo validation
- clock-skew handling
- certificate rotation
- replay prevention
- encrypted assertion support if required

Provisioning behavior:

- map identity to exactly one tenant
- support configured role and department mapping
- prevent externally asserted platform_super_admin
- support JIT provisioning only when tenant enables it
- enforce seat limits
- audit login and provisioning events

Add isolated test identity-provider fixtures.
Do not use real production credentials.