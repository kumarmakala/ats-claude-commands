Implement secure caching for parsed documents and embeddings.



Requirements:



1\. Calculate stable content hash after safe normalization.

2\. Cache parsed resume result by:

&#x20;  - tenant scope where required

&#x20;  - content hash

&#x20;  - parser version

3\. Cache JD parse and embedding by:

&#x20;  - tenant scope where required

&#x20;  - content hash

&#x20;  - model/version

4\. Never use cache keys containing PII.

5\. Encrypt or protect cached sensitive content.

6\. Define TTL and deletion behavior.

7\. Invalidate when parser/model version changes.

8\. Prevent cross-tenant content leakage.

9\. Meter cached versus uncached operations correctly.

10\. Record cache-hit metrics.



Add tests proving:

\- same content reuses cache

\- different parser version does not

\- tenant isolation remains intact

\- deletion/erasure invalidates related cache

