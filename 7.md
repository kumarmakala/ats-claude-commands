Build admin-only enterprise provisioning screens.

Screens:

1. SSO overview
2. OIDC configuration
3. SAML configuration
4. Domain verification
5. Claim mapping
6. Role and department mapping
7. SCIM configuration and token rotation
8. CSV bulk import
9. Import dry-run
10. Row-level validation results
11. Import progress
12. Seat usage and dormant-seat view

Requirements:

- display secrets only once when generated
- never write secrets to logs
- require confirmation for credential rotation
- provide metadata download where supported
- validate URLs and domains
- accessible forms
- permission-aware navigation
- robust errors and status states

Add frontend tests with mocked provider metadata.